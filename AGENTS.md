# stanzawhere

Shared Cursor project for João’s stanzawhere tool.

- Prefer small, reviewable changes.
- Do not commit secrets (`.env`, credentials, keys).
- Ask before adding dependencies or changing the tool’s public interface.
- Default remote is `ai-stanza/stanzawhere` (private).
